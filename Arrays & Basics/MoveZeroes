def moveZeroes(nums):

    j = 0

    for i in range(len(nums)):

        if nums[i] != 0:

            nums[i], nums[j] = nums[j], nums[i]

            j += 1

    return nums
explain more clearly why j is taken and everthing from scracth 
